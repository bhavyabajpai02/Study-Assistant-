services:
  - type: web
    name: aether-study-assistant
    env: node
    buildCommand: npm install && npm run build
    startCommand: node backend/server.js
    envVars:
      - key: NODE_VERSION
        value: 20.0.0
